#!/usr/bin/env bash

tabbedid=$1

bspc subscribe node_add | while read node; do
    node=$(echo $node | cut -d ' ' -f5)
    if [[ "$(bspc query -N $node -n @brother)" == $tabbedid ]]; then
        bspc config -n $node border_width 0
        xdotool windowreparent $node $tabbedid
    fi
done
