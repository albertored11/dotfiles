#!/usr/bin/env bash

sltime=30
maxtime=300000

while true; do
    sleep $sltime

    if [[ $(xprintidle) -ge $maxtime ]]; then
        lock-screen &
        sleep 0.5
        xset dpms force off
    fi
done
